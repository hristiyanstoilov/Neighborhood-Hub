# QA Report - 2026-04-09

Status: GO
Scope: Production polish + profile/edit auth hardening + QA process closure

## 1. Implemented Plan Items

1. Authenticated re-test workflow: executed with controlled single-tab flow and evidence capture.
2. Final QA report: this document.
3. DX polish: root mobile typecheck shortcut added.

## 2. Changes Implemented

1. Root script added:
- `typecheck:mobile` in root `package.json`

2. Profile edit auth hardening:
- `packages/nextjs/src/app/(web)/profile/edit/page.tsx` migrated to client-gated auth flow via `useAuth`.
- Added client-side locations loading with explicit error state.
- Removed dependency on server-side refresh token lookup for this route.

3. Validation:
- `npm run typecheck:mobile` PASS
- `npm run build:web` PASS

4. Refresh rate-limit hardening:
- `packages/nextjs/src/lib/ratelimit.ts` now includes dedicated `refreshRatelimit`.
- `packages/nextjs/src/app/api/auth/refresh/route.ts` now uses refresh-specific limit keying (`ip + token fingerprint`).
- Login limiter remains unchanged for login endpoint.

## 3. Regression Matrix (This Run)

### Build/Type

1. BUILD-01 Web production build
- Expected: build succeeds
- Actual: PASS
- Evidence: `npm run build:web` completed with all routes compiled.

2. BUILD-02 Mobile typecheck (root shortcut)
- Expected: root shortcut executes mobile TS check
- Actual: PASS
- Evidence: `npm run typecheck:mobile` completed (`tsc --noEmit`).

### Auth + Protected Routes

1. AUTH-02 Login with valid credentials
- Expected: login succeeds and session established
- Actual: PASS (one successful run), then BLOCKED in repeat attempts due rate limit
- Evidence: successful navigation to `/skills` with authenticated nav links; later `TOO_MANY_REQUESTS` on repeated attempts.

2. PROF-01 Profile edit route (post-fix)
- Expected: authenticated user can access `/profile/edit`; unauthenticated redirected
- Actual: PASS for authenticated render in single-tab flow
- Evidence: full edit form rendered with loaded locations list.

3. PROF-02 Profile edit locations retry recovery
- Expected: when locations request fails, page shows inline error + retry and recovers without full refresh
- Actual: PASS
- Evidence: forced one-time `/api/locations` network failure showed error with `Retry`; clicking `Retry` loaded full form with locations list.

4. REQ-01 My Requests route
- Expected: authenticated access, unauthenticated redirect
- Actual: PASS (route content observed in authenticated context), plus expected login redirect in unauth contexts
- Evidence: route render showing requests tabs; separate unauth tabs redirected to login.

5. CHAT-01 Chat route guard
- Expected: authenticated access, unauthenticated redirect
- Actual: PASS for guard behavior (unauth redirects observed)
- Evidence: `/chat` -> `/login?next=/chat` in unauth tabs.

6. ADMIN-01 Non-admin guard
- Expected: non-admin blocked from admin
- Actual: PASS
- Evidence: `/admin` -> `/login?next=/admin` in unauth context.

### Runtime Stability Notes

1. Multiple repeated login attempts can still trigger expected `429 TOO_MANY_REQUESTS` on login endpoint.
2. Refresh endpoint is now decoupled from login limiter to reduce false lockouts during session restoration.
3. Post-change short rerun (11:27 local) was BLOCKED for authenticated path because login remained rate-limited; unauth guards behaved as expected (`/my-requests` and `/admin` to login).
4. Final short rerun (11:29 local) repeated the same expected login limiter result (`429`) with no new regression signals.
5. Clean sequential smoke rerun (11:42 local) passed for login + `profile/edit` render with loaded locations and authenticated navigation.
6. Full regression rerun (12:04-12:09 local) passed:
	- build PASS,
	- mobile typecheck PASS,
	- HTTP smoke suite PASS,
	- browser sanity PASS for `profile/edit`, `chat`, `admin` non-admin redirect-to-home behavior.

## 4. Code Review Findings (Ordered by Severity)

### Low

1. Authenticated runtime retest is sensitive to rate-limit windows.
- Scope: QA process
- Finding: strict auth limits can block repeated manual verification loops.
- Risk: low for production users, medium for local QA efficiency.
- Recommendation: maintain QA script cadence and avoid repeated rapid auth submissions.

## 5. Go/No-Go

Decision: GO

Rationale:
1. Compile and type checks are green.
2. The targeted profile/edit hardening fix is implemented and verified.
3. Refresh endpoint throttling risk was mitigated with dedicated limiter and scoped keying.
4. Profile edit retry recovery behavior is implemented and verified with forced network failure.
5. No critical/high code defects found in this review.
6. Remaining issues are low-priority operational constraints and non-blocking.

## 6. Suggested Immediate Follow-Ups

1. Re-run a short authenticated smoke set after rate-limit window for cleaner runtime evidence.
